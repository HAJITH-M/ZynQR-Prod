/**
 * Shared dashboard content inset: max width, horizontal padding, top/bottom padding.
 * Use `DASHBOARD_PAGE_SHELL` when the page is a simple vertical stack of major blocks.
 */
export const DASHBOARD_PAGE_INSET = "mx-auto w-full max-w-7xl px-4 pb-16 pt-8 md:px-8";

export const DASHBOARD_PAGE_SHELL = `${DASHBOARD_PAGE_INSET} space-y-8`;
